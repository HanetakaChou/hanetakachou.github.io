[Steam Controller](https://wiki.archlinux.org/title/Gamepad#Steam_Controller)  
The following command may need to be run with root privileges to make the controller available  
```bash
udevadm trigger 
```